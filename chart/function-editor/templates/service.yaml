apiVersion: v1
kind: Service
metadata:
  name: {{ include "function-editor.fullname" . }}
  labels:
    {{- include "function-editor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "function-editor.selectorLabels" . | nindent 4 }}
